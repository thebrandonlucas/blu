# Safety Rules

- Never write outside the project root.
- Never edit files under `_site/` — they are generated output.
- Do not modify deployment workflows without explicit confirmation.
- Keep `config.json` changes deliberate — it affects RSS feeds and site metadata.
- Treat `_resources/` assets carefully — fonts and images are referenced by URL.
